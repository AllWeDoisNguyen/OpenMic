<%= form_for @booking do |f| %>
  <% if @booking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@booking.errors.count, "error") %> prohibited this booking from being saved:</h2>

      <ul>
      <% @booking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comedian_Username %><br>
    <%= f.collection_select(:comedian_id, @comedians, :id, :username, :prompt => true) %>
  </div>
  <div class="field">
    <%= f.label :show_Name %><br>
    <%= f.collection_select(:show_id, @shows, :id, :show_name, :prompt => true) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
